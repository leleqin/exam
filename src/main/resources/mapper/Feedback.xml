<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.eurasia.dao.FeedbackDao">

    <select id="feedback" parameterType="edu.eurasia.entity.Feedback" resultType="String">
        select #{rank} from grade;
    </select>
</mapper>